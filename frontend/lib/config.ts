// Central config — update this file to change branding across the app
const config = {
  appName:      "Homly",
  tagline:      "Your household, run by AI — right inside WhatsApp.",
  description:  "Homly reads every receipt your helper sends, tracks spending, budgets, insurance, chores, and pantry — and you can just ask it questions in the group instead of digging through a dashboard.",
  elevator:     "An AI assistant that lives in your household WhatsApp group — expenses, budgets, insurance, chores, and pantry, all understood automatically.",
  twitterHandle: "",
  supportEmail: "",
  siteUrl:      process.env.NEXT_PUBLIC_SITE_URL || "https://homly-six.vercel.app",
};

export default config;
